# ขั้นตอนต่อไปสำหรับ funaging.club

## ✅ สถานะปัจจุบัน
- ลบ `funaging.studio` แล้ว
- เหลือแค่ `funaging.club` ใน Vercel
- DNS ตั้งค่าแล้ว (76.76.21.21)

## 📋 สิ่งที่ต้องทำ

### 1. ตรวจสอบ DNS ใน Cloudflare
ตรวจสอบว่า DNS records ตั้งค่าถูกต้อง:

**ไปที่:** https://dash.cloudflare.com
1. เลือก domain `funaging.club`
2. ไปที่ **DNS** → **Records**
3. ตรวจสอบว่ามี A Record:
   - Type: **A**
   - Name: **@** (หรือ `funaging.club`)
   - IPv4: **76.76.21.21**
   - **Proxy: DNS only** (ปิด proxy - ไม่เปิดสีส้ม) ⚠️ สำคัญ!

### 2. รอ Vercel Verify
- Vercel จะ verify domain อัตโนมัติ
- ใช้เวลา **5-30 นาที** หลังจากตั้งค่า DNS
- Vercel จะส่ง **email** เมื่อ domain พร้อมใช้งาน

### 3. ตรวจสอบสถานะ (Optional)
```powershell
# ตรวจสอบ domain
vercel domains inspect funaging.club

# ตรวจสอบ DNS
nslookup funaging.club
```

---

## ⚠️ หมายเหตุสำคัญ

**Proxy status ต้องเป็น DNS only** (ไม่เปิด proxy - ไม่มีสีส้ม)
- ถ้าเปิด proxy (สีส้ม) Vercel จะ verify domain ไม่ได้
- ต้องปิด proxy เพื่อให้ Vercel verify ได้

---

## ✅ หลังจาก Vercel Verify เสร็จ

เว็บจะทำงานที่:
- https://funaging.club
- https://www.funaging.club (auto-redirect)

---

## สรุป

**ต้องทำ:**
1. ✅ ตรวจสอบ DNS ใน Cloudflare (Proxy: DNS only)
2. ⏳ รอ Vercel verify (5-30 นาที)

**ไม่ต้องทำ:**
- ไม่ต้องตั้งค่า DNS ใหม่ (ตั้งค่าแล้ว)
- ไม่ต้องเพิ่ม domain ใหม่ (มีแล้ว)



